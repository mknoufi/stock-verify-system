# ============================================================================
# Stock Verify Backend - Docker Compose Overrides (Non-secret)
# ============================================================================
# Used by `docker-compose.yml` to override `.env.example` for container networking.
# ============================================================================

# Docker service DNS name
MONGO_URL=mongodb://mongo:27017

# Backend listen port (compose maps 8001 -> 8001)
PORT=8001
