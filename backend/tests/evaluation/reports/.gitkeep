# Evaluation Reports

This directory contains generated evaluation reports.

Reports are named with timestamps: `evaluation_report_YYYYMMDD_HHMMSS.{json,md}`

## Generating Reports

```bash
# JSON format (default)
make eval

# Markdown format
make eval-report

# Custom output
python -m backend.tests.evaluation.run_evaluation --all --format md --output ./reports
```
